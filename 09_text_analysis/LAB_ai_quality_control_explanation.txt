I modified the quality control prompt to require strict JSON-only output with no extra text, and I reduced the details field to 0-30 words for more consistent parsing. I used Ollama with a locally available model (llama3.2:1b) and ran the script to produce boolean accuracy, six Likert scores, and an overall score. Compared with manual quality control, the AI approach is faster and gives a structured multi-criteria evaluation in one pass, while manual checks are more transparent and deterministic. What worked well was the stricter format instruction; next I would improve prompt calibration so relevance and succinctness scores are more stable across repeated runs.
